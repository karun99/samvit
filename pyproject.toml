[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "samvit"
version = "1.0.0"
description = "Samvit — local-first personal AI with VISION accuracy marker and ULTRON error validation constraint. Standard library only; optional voice input requires the whisper binary."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "Sai Karun Nandipati", email = "saikarun085@gmail.com" }]
keywords = ["local-first", "personal-ai", "guardrails", "memory-graph", "sqlite", "accuracy-marker"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3 :: Only",
]

[project.scripts]
samvit = "samvit.cli:main"

[project.optional-dependencies]
dev = ["pytest>=7", "pytest-cov>=4.1"]

[tool.setuptools.packages.find]
include = ["samvit*"]